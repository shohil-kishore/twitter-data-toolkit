<% include ./partials/header.ejs %>

  <body>
    <div class="container">
      
      <div style="padding: 40px;" class="display-4 text-center">
        Bearer Token Generator
      </div>
      
      <% if (bearerToken !== undefined) { %>
        <div class="alert alert-success overflow-auto" role="alert">
          <p><%= bearerToken %></p>
        </div>
      <% } %>

      <form action="/generate-token" method="POST">
        <div class="form-group">
          <label for="publicKey">Consumer Key</label>
          <input type="text" class="form-control" name="publicKey" />
          <small id="publicKeyHelp" class="form-text text-muted">Find your keys here: dev dashboard > apps > app details > keys and tokens.</small>
        </div>
        <div class="form-group">
          <label for="privateKey">Consumer Secret Key</label>
          <input type="text" class="form-control" name="secretKey" />
          <small id="publicKeyHelp" class="form-text text-muted">Never share your secret key with others.</small>
        </div>
        <input
          class="btn btn-primary"
          type="submit"
          name="submit"
          value="Submit"
        />      
      </form>
    </div>
  </body>
</html>